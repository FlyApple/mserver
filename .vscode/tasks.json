{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "go",
            "args": [
                "build",
                "-o", "bin/server",
                "src/main.go"
            ]
        },
        {
            "label": "build test",
            "type": "shell",
            "command": "go",
            "args": [
                "build",
                "-o", "bin/test",
                "test/test.go"
            ]
        }
    ]
}
